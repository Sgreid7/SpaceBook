import React from "react"
import Layout from "../components/layout"
import styled from "styled-components"
import { Link } from "gatsby"

const satellites = () => {
  return (
    <Layout>
      <div>SATELLITES PAGE</div>
    </Layout>
  )
}

export default satellites
